{
    # HTTP-only — .homelab.local is not a public domain; ACME unavailable.
    auto_https off
    admin off
}

http://pihole.homelab.local {
    reverse_proxy pihole:80
}
http://grafana.homelab.local {
    reverse_proxy {env.IP_OBSERVE}:3000
}
http://prometheus.homelab.local {
    reverse_proxy {env.IP_OBSERVE}:9090
}
http://alertmanager.homelab.local {
    reverse_proxy {env.IP_OBSERVE}:9093
}
http://uptime.homelab.local {
    reverse_proxy {env.IP_OBSERVE}:3001
}
http://portainer.homelab.local {
    reverse_proxy {env.IP_OBSERVE}:9000
}
http://ntfy.homelab.local {
    reverse_proxy {env.IP_OBSERVE}:8085
}
http://n8n.homelab.local {
    reverse_proxy {env.IP_SVC_01}:5678
}
http://camunda.homelab.local {
    reverse_proxy {env.IP_SVC_01}:8080
}
http://greentechhub.homelab.local {
    reverse_proxy {env.IP_SVC_02}:8000
}
http://jellyfin.homelab.local {
    reverse_proxy {env.IP_SVC_03}:8096
}

### Tailscale Routes ###
# Infisical
https://homelab-edge.{$TAILNET}.ts.net:8443 {
    tls /certs/homelab-edge.{$TAILNET}.ts.net.crt /certs/homelab-edge.{$TAILNET}.ts.net.key
    reverse_proxy host.docker.internal:8222
}

# Semaphore
https://homelab-edge.{$TAILNET}.ts.net:8444 {
    tls /certs/homelab-edge.{$TAILNET}.ts.net.crt /certs/homelab-edge.{$TAILNET}.ts.net.key
    reverse_proxy host.docker.internal:3010
}

# Grafana
https://homelab-edge.{$TAILNET}.ts.net:3000 {
    tls /certs/homelab-edge.{$TAILNET}.ts.net.crt /certs/homelab-edge.{$TAILNET}.ts.net.key
    reverse_proxy {env.IP_OBSERVE}:3000
}

# Prometheus
https://homelab-edge.{$TAILNET}.ts.net:9090 {
    tls /certs/homelab-edge.{$TAILNET}.ts.net.crt /certs/homelab-edge.{$TAILNET}.ts.net.key
    reverse_proxy {env.IP_OBSERVE}:9090
}

# Alertmanager
https://homelab-edge.{$TAILNET}.ts.net:9093 {
    tls /certs/homelab-edge.{$TAILNET}.ts.net.crt /certs/homelab-edge.{$TAILNET}.ts.net.key
    reverse_proxy {env.IP_OBSERVE}:9093
}

# Uptime Kuma
https://homelab-edge.{$TAILNET}.ts.net:3001 {
    tls /certs/homelab-edge.{$TAILNET}.ts.net.crt /certs/homelab-edge.{$TAILNET}.ts.net.key
    reverse_proxy {env.IP_OBSERVE}:3001
}

# Portainer
https://homelab-edge.{$TAILNET}.ts.net:9000 {
    tls /certs/homelab-edge.{$TAILNET}.ts.net.crt /certs/homelab-edge.{$TAILNET}.ts.net.key
    reverse_proxy {env.IP_OBSERVE}:9000
}

# ntfy
https://homelab-edge.{$TAILNET}.ts.net:8085 {
    tls /certs/homelab-edge.{$TAILNET}.ts.net.crt /certs/homelab-edge.{$TAILNET}.ts.net.key
    reverse_proxy {env.IP_OBSERVE}:8085
}
